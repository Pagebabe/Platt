document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('#profileRoot');if(!root)return;const sb=window.LIVARA_SUPABASE,id=new URLSearchParams(location.search).get('id')||'';const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;let invalid=false;function render(message='Dieses Profil existiert nicht oder ist nicht öffentlich verfügbar.'){invalid=true;if(root.dataset.notFound==='1')return;root.dataset.notFound='1';root.innerHTML=`<section class="panel profile-not-found"><div class="eyebrow soft">Profil nicht gefunden</div><h1>Dieses Profil ist nicht verfügbar.</h1><p class="muted">${message}</p><a class="btn btn-primary" href="index.html">Zurück zu Entdecken</a></section>`;document.querySelectorAll('.profile-section').forEach(s=>s.classList.add('hidden'));document.querySelector('#mobileProfileCta')?.classList.add('hidden')}if(!id||!uuid.test(id)){render();return}const observer=new MutationObserver(()=>{if(invalid&&root.dataset.notFound!=='1'){root.dataset.notFound='';render()}});observer.observe(root,{childList:true,subtree:true});(async()=>{try{const r=await fetch(sb.url+'/rest/v1/listings?id=eq.'+encodeURIComponent(id)+'&status=eq.active&select=id',{headers:{apikey:sb.key,Authorization:'Bearer '+sb.key}});if(!r.ok)throw new Error();const rows=await r.json();if(!rows.length)render()}catch{render('Das Profil konnte nicht zuverlässig geladen werden. Bitte versuche es später erneut.')}})()});
