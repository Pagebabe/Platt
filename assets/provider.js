document.addEventListener('DOMContentLoaded',async()=>{const A=window.LivaraAuth;document.querySelector('#logoutBtn').onclick=A.logout;const msg=document.querySelector('#formMessage');const availabilityMsg=document.querySelector('#availabilityMessage');const saveAvailabilityBtn=document.querySelector('#saveAvailabilityBtn');const availabilityHint=document.querySelector('#availabilityHint');const days=['Mo','Di','Mi','Do','Fr','Sa','So'];const show=(t,bad=false)=>{msg.textContent=t;msg.className='notice '+(bad?'danger':'good')};const showAvailability=(t,bad=false)=>{availabilityMsg.textContent=t;availabilityMsg.className='notice '+(bad?'danger':'good')};function renderAvailabilityEditor(rows=[]){const byDay=new Map(rows.map(r=>[Number(r.weekday),r]));document.querySelector('#availabilityEditor').innerHTML=days.map((day,i)=>{const r=byDay.get(i);const on=!!r?.is_available;const start=String(r?.starts_at||'10:00').slice(0,5);const end=String(r?.ends_at||'20:00').slice(0,5);return `<div class="availability-row ${on?'':'inactive'}" data-weekday="${i}"><strong>${day}</strong><input class="field starts" type="time" value="${start}" ${on?'':'disabled'} aria-label="${day} Start"><input class="field ends" type="time" value="${end}" ${on?'':'disabled'} aria-label="${day} Ende"><label class="day-toggle"><input class="enabled" type="checkbox" ${on?'checked':''}> verfügbar</label></div>`}).join('');document.querySelectorAll('.availability-row .enabled').forEach(box=>box.addEventListener('change',()=>{const row=box.closest('.availability-row');row.classList.toggle('inactive',!box.checked);row.querySelector('.starts').disabled=!box.checked;row.querySelector('.ends').disabled=!box.checked}))}renderAvailabilityEditor();try{const ctx=await A.requireRoles(['provider','studio','admin']);const id=new URLSearchParams(location.search).get('id');let current=null;async function loadAvailability(){if(!current){saveAvailabilityBtn.disabled=true;availabilityHint.textContent='Speichere zuerst dein Profil.';renderAvailabilityEditor();return}saveAvailabilityBtn.disabled=false;availabilityHint.textContent='Ein Zeitfenster pro Wochentag · jederzeit änderbar';const q=await A.rest('availability?listing_id=eq.'+encodeURIComponent(current.id)+'&select=weekday,starts_at,ends_at,is_available&order=weekday.asc');renderAvailabilityEditor(q.data||[])}if(id){const q=await A.rest('listings?id=eq.'+encodeURIComponent(id)+'&owner_id=eq.'+ctx.user.id+'&select=*');current=q.data?.[0]||null}if(current){document.querySelector('#formTitle').textContent='Profil bearbeiten';document.querySelector('#listingStatus').textContent=current.status;for(const [k,v] of Object.entries({name:current.name,category:current.category,city:current.city,district:current.district||'',price:current.price_from??'',description:current.description||''}))document.querySelector('#'+k).value=v;document.querySelector('#available').checked=!!current.available_today}await loadAvailability();document.querySelector('#listingForm').onsubmit=async e=>{e.preventDefault();try{const body={name:document.querySelector('#name').value.trim(),category:document.querySelector('#category').value,city:document.querySelector('#city').value.trim(),district:document.querySelector('#district').value.trim()||null,description:document.querySelector('#description').value.trim()||null,price_from:document.querySelector('#price').value?Number(document.querySelector('#price').value):null,available_today:document.querySelector('#available').checked};if(current){await A.rest('listings?id=eq.'+encodeURIComponent(current.id),{method:'PATCH',body,prefer:'return=minimal'});show('Profil gespeichert. Moderationsstatus bleibt geschützt.')}else{body.owner_id=ctx.user.id;body.slug=body.name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+ctx.user.id.slice(0,8)+'-'+Date.now().toString(36);const r=await A.rest('listings',{method:'POST',body,prefer:'return=representation'});current=r.data?.[0]||null;show('Profil als Entwurf angelegt. Jetzt kannst du den Wochenplan speichern.');if(current){history.replaceState(null,'','provider.html?id='+encodeURIComponent(current.id));await loadAvailability()}}}catch(err){show(err.message||'Speichern fehlgeschlagen',true)}};saveAvailabilityBtn.onclick=async()=>{if(!current){showAvailability('Speichere zuerst das Profil.',true);return}try{const slots=[];for(const row of document.querySelectorAll('.availability-row')){const enabled=row.querySelector('.enabled').checked;if(!enabled)continue;const starts=row.querySelector('.starts').value;const ends=row.querySelector('.ends').value;if(!starts||!ends||starts>=ends)throw new Error(days[Number(row.dataset.weekday)]+': Endzeit muss nach der Startzeit liegen.');slots.push({listing_id:current.id,weekday:Number(row.dataset.weekday),starts_at:starts,ends_at:ends,is_available:true})}await A.rest('availability?listing_id=eq.'+encodeURIComponent(current.id),{method:'DELETE',prefer:'return=minimal'});if(slots.length)await A.rest('availability',{method:'POST',body:slots,prefer:'return=minimal'});const today=(new Date().getDay()+6)%7;const todayAvailable=slots.some(s=>s.weekday===today);await A.rest('listings?id=eq.'+encodeURIComponent(current.id),{method:'PATCH',body:{available_today:todayAvailable},prefer:'return=minimal'});document.querySelector('#available').checked=todayAvailable;showAvailability('Wochenplan gespeichert. Die Profilseite verwendet jetzt diese Zeiten.')}catch(err){showAvailability(err.message||'Wochenplan konnte nicht gespeichert werden.',true)}}}catch(e){if(e.message!=='redirect')show('Zugriff nicht möglich.',true)}});